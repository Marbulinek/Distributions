namespace Distributions
{
    /// <summary>
    /// Represents a distribution interface that provides a method to get a distribution value.
    /// </summary>
    public interface IDistribution
    {
        /// <summary>
        /// Represents a distribution interface that provides a method to get a distribution value.
        /// </summary>
        double GetDistribution();
    }
}
